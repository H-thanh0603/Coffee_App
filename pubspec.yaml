name: smartcafe
description: SmartCafe - He thong quan li ban cafe, nuoc uong thong minh
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=3.10.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6
  provider: ^6.1.1
  go_router: ^13.0.0
  intl: ^0.19.0
  fl_chart: ^0.66.0
  uuid: ^4.2.1
  google_fonts: ^6.1.0
  collection: ^1.18.0
  shared_preferences: ^2.5.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
